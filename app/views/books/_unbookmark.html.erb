<%= link_to bookmark_path(current_user.bookmarks.find_by(book_id: book.id)), id: "unbookmark-button-for-book-#{book.id}", data: { turbo_method: :delete } do %>
  <i class="bi bi-star-fill"></i>
<% end %>
